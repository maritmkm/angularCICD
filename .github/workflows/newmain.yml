# name: Build and Deploy Angular to GitHub Pages

# on:
#   push:
#     branches:
#       - main

# permissions:
#   contents: write

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 18

#       - name: Install dependencies
#         run: npm ci

#       - name: Build Angular app
#         run: npm run build -- --configuration production --base-href "/angularCICD/"

#       - name: Deploy to GitHub Pages
#         uses: JamesIves/github-pages-deploy-action@v4
#         with:
#           branch: gh-pages
#           folder: dist/angularCICD/browser

name: Build, Test, and Deploy Angular (GitHub Pages + Private Docker Hub)

on:
  push:
    branches:
      - main

permissions:
  contents: write
  packages: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js for Angular
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Run security audit (non-blocking)
      - name: Run npm security audit
        run: npm audit --audit-level=high || true

      # 5️⃣ Build Angular app
      - name: Build Angular app
        run: npm run build -- --configuration production --base-href "/angularCICD/"

      # 6️⃣ Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist/angularCICD/browser

      # 7️⃣ Login to Docker Hub (private repo)
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 8️⃣ Build Docker image
      - name: Build Docker image
        run: |
          IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/angularcicd
          docker build -t $IMAGE_NAME:latest -t $IMAGE_NAME:${{ github.sha }} .

      # 9️⃣ Run Docker container test
      - name: Run Docker container for testing
        run: |
          IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/angularcicd
          docker run -d -p 8080:80 --name angular-test $IMAGE_NAME:latest
          echo "Waiting for container startup..."
          sleep 5
          echo "Checking if the app responds..."
          curl -I http://localhost:8080 || (echo "❌ Container test failed!" && exit 1)
          docker stop angular-test
          docker rm angular-test
          echo "✅ Container test passed successfully!"

      # 🔟 Push Docker image to private Docker Hub
      - name: Push Docker image
        run: |
          IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/angularcicd
          docker push $IMAGE_NAME:latest
          docker push $IMAGE_NAME:${{ github.sha }}
